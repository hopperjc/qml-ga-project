# ZZ feature map com phase damping APÓS o feature map (γ=0.05) — regime moderado.
feature_map:
  type: "zz"
  params:
    reps: 2
    entanglement: "full"
  noise:
    after_feature_map:
      type: phase_damping
      gamma: 0.05
