# configs/feature_maps/amplitude.yaml
# Baseline limpa (sem ruído). Use os arquivos em noisy_examples/ para configs com ruído.
feature_map:
  type: "amplitude"
  params:
    normalize: true

  # Schema opcional (omitir bloco abaixo desativa ruído e mantém default.qubit):
  # noise:
  #   after_feature_map: { type: amplitude_damping, gamma: 0.01 }
  #   per_ansatz_layer:  { type: amplitude_damping, gamma: 0.005 }
