# configs/feature_maps/zz.yaml
# Baseline limpa (sem ruído). Use os arquivos em noisy_examples/ para configs com ruído.
feature_map:
  type: "zz"
  params:
    reps: 2
    entanglement: "full"   # "full" ou "linear"

  # Schema opcional (omitir bloco abaixo desativa ruído e mantém default.qubit):
  # noise:
  #   after_feature_map: { type: phase_damping, gamma: 0.01 }
  #   per_ansatz_layer:  { type: phase_damping, gamma: 0.005 }
